name: livocut
description: LivoCut video segment cutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  file_picker: ^11.0.2
  ffmpeg_kit_flutter_new: ^4.2.1
  flutter:
    sdk: flutter
  path: ^1.9.1
  permission_handler: ^12.0.3
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
